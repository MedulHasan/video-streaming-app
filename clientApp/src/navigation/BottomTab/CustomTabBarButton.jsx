import React from 'react';
import {Pressable} from 'react-native';

const CustomTabBarButton = ({children, onPress}) => {
  return <Pressable onPress={() => {}}>{children}</Pressable>;
};

export default CustomTabBarButton;
